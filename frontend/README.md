# Frontend — Campus Marketplace

Quick scaffold using Vite + React. Focused pages: Login, Register, Listings, Listing detail, Create listing.

Install & run:

```bash
cd frontend
npm install
npm run dev
```

Defaults to calling backend at `http://localhost:5050/api`. To change, set `VITE_API_BASE`.
